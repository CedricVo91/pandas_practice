# pandas
# numpy
#if I create a conda environment, do I need to install all the data science packages like pandas etc again even though they are already installed in my "global" conda environment
#if yes, is it correct to use conda install pandas?
import pandas as pd
import numpy as np

print("hello conda environment and pandas")


array1 = np.array([1,2,3,4])
print(f"I will print my first array {array1}")




